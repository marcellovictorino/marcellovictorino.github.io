<figure class="{{ include.class }}">
  <img src=
    {% if jekyll.environment == 'production' %}
      "{{ include.image_path | site.url }}"
    {% else %}
      "{{ include.image_path | relative_url }}"
    {% endif %}

    {% if include.alt %}
    alt="{{ include.alt }}">
    {% else %}
    alt="{{ include.caption }}">
    {% endif %}

  {% if include.caption %}
    <figcaption>{{ include.caption | markdownify | remove: "<p>" | remove: "</p>" }}</figcaption>
  {% endif %}
</figure>
